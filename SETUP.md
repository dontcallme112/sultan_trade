# 🚀 Инструкция по запуску LUXE

## Требования

- Node.js версии 16.0 или выше
- npm или yarn

## Установка и запуск

### 1. Установите зависимости

```bash
npm install
```

или

```bash
yarn install
```

### 2. Запустите проект в режиме разработки

```bash
npm run dev
```

или

```bash
yarn dev
```

Приложение автоматически откроется в браузере по адресу `http://localhost:5173`

### 3. Сборка для продакшена

```bash
npm run build
```

Собранные файлы появятся в папке `dist/`

### 4. Предпросмотр продакшен-сборки

```bash
npm run preview
```

## 📂 Структура папок проекта

```
luxe-store/
├── public/                 # Статические файлы
├── src/
│   ├── api/               # API интеграция
│   │   ├── client.js     # Axios конфигурация
│   │   └── services/     # API сервисы
│   │       └── products.js
│   │
│   ├── components/        # React компоненты
│   │   ├── common/       # Общие UI компоненты
│   │   │   ├── Toast/
│   │   │   └── Loader/
│   │   ├── layout/       # Layout компоненты
│   │   │   ├── Header/
│   │   │   └── Footer/
│   │   └── features/     # Функциональные компоненты
│   │       └── ProductCard/
│   │
│   ├── pages/            # Страницы
│   │   ├── Home/
│   │   ├── Catalog/
│   │   ├── Product/
│   │   └── Cart/
│   │
│   ├── hooks/            # Custom React hooks
│   │   ├── useProducts.js
│   │   └── useProduct.js
│   │
│   ├── context/          # React Context
│   │   └── CartContext.jsx
│   │
│   ├── styles/           # Глобальные стили
│   │   ├── globals.css
│   │   ├── variables.css
│   │   └── animations.css
│   │
│   ├── utils/            # Утилиты
│   │   ├── formatters.js
│   │   └── constants.js
│   │
│   ├── App.jsx           # Главный компонент
│   └── main.jsx          # Точка входа
│
├── index.html            # HTML шаблон
├── vite.config.js        # Конфигурация Vite
├── package.json          # Зависимости
└── README.md             # Документация
```

## 🔧 Настройка API

### Текущее API (Fake Store API)

По умолчанию используется публичное API для демонстрации:
- URL: `https://fakestoreapi.com`
- Документация: https://fakestoreapi.com/docs

### Подключение своего API

Откройте файл `src/api/client.js` и измените `baseURL`:

```javascript
const apiClient = axios.create({
  baseURL: 'https://your-api.com/api', // Ваш URL
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
});
```

### Добавление авторизации

Если ваш API требует токен авторизации, раскомментируйте код в `src/api/client.js`:

```javascript
// Request interceptor
apiClient.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('auth-token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);
```

## 🎨 Кастомизация дизайна

### Изменение цветовой схемы

Откройте `src/styles/variables.css` и измените CSS переменные:

```css
:root {
  /* Основные цвета */
  --bg-primary: #0a0a0f;        /* Темный фон */
  --accent-gold: #d4af37;       /* Золотой акцент */
  
  /* Измените на свои цвета */
  --bg-primary: #ваш-цвет;
  --accent-gold: #ваш-цвет;
}
```

### Изменение шрифтов

В `src/styles/variables.css`:

```css
:root {
  --font-display: 'Playfair Display', serif;
  --font-body: 'Inter', sans-serif;
  
  /* Измените на свои шрифты */
  --font-display: 'Ваш шрифт', serif;
  --font-body: 'Ваш шрифт', sans-serif;
}
```

Не забудьте импортировать новые шрифты в `src/styles/globals.css`.

## 📱 Адаптивность

Проект полностью адаптивен для всех устройств:

- **Mobile**: < 768px
- **Tablet**: 768px - 1024px  
- **Desktop**: > 1024px

## 🛠️ Доступные скрипты

- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка для продакшена
- `npm run preview` - Предпросмотр продакшен-сборки
- `npm run lint` - Проверка кода ESLint

## 🔍 Основные фичи

✅ Темная премиум-тема с золотыми акцентами  
✅ Полностью адаптивный дизайн  
✅ Корзина с сохранением в localStorage  
✅ Фильтрация и сортировка товаров  
✅ Детальная страница товара  
✅ Toast уведомления  
✅ Skeleton loaders для улучшенного UX  
✅ Плавные анимации  
✅ SEO-оптимизация  

## 🐛 Возможные проблемы

### Ошибка при установке зависимостей

Попробуйте очистить кэш npm:
```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### Ошибки при сборке

Убедитесь, что используете Node.js версии 16+:
```bash
node --version
```

### API не работает

Проверьте:
1. Подключение к интернету
2. Доступность API по указанному URL
3. CORS настройки вашего API

## 📞 Поддержка

Если у вас возникли вопросы:
1. Проверьте документацию
2. Посмотрите комментарии в коде
3. Создайте issue в репозитории

## 📄 Лицензия

MIT License - свободно используйте в своих проектах

---

**Готово к использованию! 🎉**